
<?php 
$staff_id = escape($user->data()->staff_id);
$username = escape($user->data()->username);
$title = escape($user->data()->title);
$firstname = escape($user->data()->firstname);
$lastname = escape($user->data()->lastname);
$dob = escape($user->data()->dob);
$phone = escape($user->data()->phone);
$sog = escape($user->data()->sog);
$email = escape($user->data()->email);
$street = escape($user->data()->street);
$city = escape($user->data()->city);
$state = escape($user->data()->state);
$department = escape($user->data()->department);
$joined = escape($user->data()->joined);
$bio = escape($user->data()->bio);
$userimg = escape($user->data()->image);
?>



